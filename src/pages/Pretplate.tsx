import { CreditCard } from 'lucide-react'
import { PagePlaceholder } from '@/components/PagePlaceholder'

export default function Pretplate() {
  return (
    <PagePlaceholder
      title="Pretplate"
      subtitle="FitLink pretplate trenera, status naplate i istorija."
      icon={CreditCard}
    />
  )
}
