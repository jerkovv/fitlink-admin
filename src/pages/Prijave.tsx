import { Flag } from 'lucide-react'
import { PagePlaceholder } from '@/components/PagePlaceholder'

export default function Prijave() {
  return (
    <PagePlaceholder
      title="Prijave"
      subtitle="Prijave korisnika i moderacija sadržaja."
      icon={Flag}
    />
  )
}
